# ANTBAR Agent Direction

这是 `antbarweb` 后续给 Codex、Claude Code、Cursor 或其它 agent 使用的唯一主方向文档。

如果其它旧文档与本文冲突，以本文为准。

---

## 1. 当前项目真实状态

`antbarweb` 已经不是原始镜像项目，也不是要重新从 mirror 里继续抽页面的项目。

当前事实：

1. 产品详情页已经完成去 mirror 化。
2. 产品页 CSS 已经合并进项目。
3. 产品页 JS 已经合并进项目。
4. Blog 详情内容已经清洗成 Markdown。
5. Review 详情内容已经清洗成 Markdown。
6. 后续重点不是重做产品详情页，而是统一站点结构、验证弹窗、导航、列表页、文章模板、品牌页和构建稳定性。

后续 agent 不要再把产品详情页当成“仍然依赖 mirror 的页面”来处理。

---